repos:
  primary: "."
  additional:
    - "/Users/malte/.beads-planning"
sync-branch: beads-sync

dolt.shared-server: false

# Disable JSONL auto-import; Dolt is the source of truth.
no-auto-import: true

import.auto: false

export.auto: false

export.git-add: false
